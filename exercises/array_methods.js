const myList = ["hello", "hola"];

// Constant - Just put it in the end
myList.push("bonjour");

// Linear -> Moving one position all the elements
myList.unshift();

// Linear -> Moving one position all the elements
myList.shift();
